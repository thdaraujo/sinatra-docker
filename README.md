Sinatra Docker
==============

Based on this [repo](https://github.com/jasonswett/dockerized-sinatra-application)

To run locally:

``` sh
$ bundle install
$ ruby hello.rb
$ open localhost:4567
```

Run:
```sh
$ docker-compose up
```

Open:
``` sh
$ open localhost
```
